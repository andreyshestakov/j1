kind: trusted_cluster
version: v2
metadata:
  name: {{ cluster_name }}
spec:
  enabled: true
  token: {{ cluster_token }}
  tunnel_addr: {{ cluster_name }}:3024
  web_proxy_addr: {{ cluster_name }}:3080
  role_map:
   - remote: "admin"
     local: ["admin"]
